{
  "scripts": {
    "build": "esbuild src/content.ts --bundle --outfile=dist/content.js --format=iife --platform=browser && sed \"s/__VERSION__/${VERSION}/\" manifest.json.tmpl > manifest.json",
    "test": "node --test --import tsx",
    "fmt": "prettier --write ."
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^25.1.0",
    "esbuild": "^0.27.2",
    "prettier": "^3.8.1",
    "tsx": "^4.21.0"
  }
}
